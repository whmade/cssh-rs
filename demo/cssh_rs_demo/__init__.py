"""Scripted screen-recording demo for cssh-rs.

Composes the shared cssh_rs_automation primitives (screen recorder, keystroke
driver, window focus, sshd fixture, config generator) into a self-contained,
reproducible demo clip. Runs on Windows only, like the automation harness it
drives.
"""
