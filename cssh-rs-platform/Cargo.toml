[package]
name = "cssh-rs-platform"
version.workspace = true
edition = "2021"
license-file = "../LICENSE.txt"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage,coverage_nightly)'] }
